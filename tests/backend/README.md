# Backend Tests

| Subdir | Scope |
|--------|-------|
| `auth-service/` | FastAPI auth (JWT, RBAC, users, roles) |
| `core/` | Go API (ACL logic, handlers) |

See each subdir's README for run commands.
